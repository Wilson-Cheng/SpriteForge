# Samples

This directory holds example SpriteForge projects and Spine bundles
(`*.sfproj`, `*.json` + `*.atlas` + `*.png`) that can be loaded into the
editor or the standalone runtime player.

It is intentionally empty in the initial open-source release. Sample
content will be added in a follow-up commit.
